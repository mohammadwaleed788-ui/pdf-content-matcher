# Copy this file to .env.development (for local dev) and .env.production (for builds)
# Then fill in the values appropriate for each environment.

# Backend API URL.
# - Local dev: http://localhost:5000
# - Production with nginx reverse proxy on the same origin: leave empty
# - Production with direct backend exposure: http://your-server-ip:PORT
VITE_API_URL=
